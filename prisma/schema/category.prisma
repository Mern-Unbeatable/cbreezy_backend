model Category {
  id            String        @id @default(cuid())
  name          String        @unique
  image         String?       
  type          ListingType   // SERVICE or EVENT
  createdAt     DateTime      @default(now())
  subCategories SubCategory[]
  listings      Listing[]
}